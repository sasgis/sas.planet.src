
{.$DEFINE USE_STATIC_SQLITE3}

{.$DEFINE ENABLE_ZEOS_DBMS}
{$DEFINE ENABLE_ODBC_DBMS}

{$IF DEFINED(ENABLE_ZEOS_DBMS) OR DEFINED(ENABLE_ODBC_DBMS)}
  {$DEFINE ENABLE_DBMS}
{$IFEND}

{$IFDEF DEBUG}

  {.$DEFINE ORM_LOG_ENABLE}
  
  {$IFDEF ORM_LOG_ENABLE}
    
    {.$DEFINE ORM_LOG_VERBOSE}
    
    {$IFDEF ORM_LOG_VERBOSE}
      {$DEFINE ORM_LOG_CACHE_RESULT}
      {$DEFINE ORM_LOG_CACHE_ENTER}
    {$ENDIF}
    
    {$DEFINE ORM_LOG_CACHE_SIZE}
    
  {$ENDIF}
  
{$ENDIF}
